{% extends "base.html.twig" %}

{% block body %}
    {% if not app.user %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    {% else %}
        <script>
            location.href = "/"
        </script>
    {% endif %}
{% endblock %}