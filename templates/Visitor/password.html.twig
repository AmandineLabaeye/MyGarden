{% extends "base.html.twig" %}

{% block body %}
    {% if not app.user %}
        {{ form(form) }}
    {% else %}
        <script>
            location.href = "/"
        </script>
    {% endif %}
{% endblock %}