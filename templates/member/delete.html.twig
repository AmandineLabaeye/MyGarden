<form method="post" action="{{ path('users_delete_member', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete your account?');">
    <button class="btn">Delete</button>
</form>
