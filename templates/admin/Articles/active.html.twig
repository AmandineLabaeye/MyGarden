{% extends "base.html.twig" %}

{% block body %}
    {% for article in articles %}
        <div class="CarteArticle">
            <p class="NameU"><span class="Categorie">Nom</span> {{ article.name }}</p>
            <p class="NameU"><span class="Categorie">Photo</span> {{ article.picture }}</p>
            <p class="DescriptionA"><span class="Categorie">Description</span> {{ article.description }}</p>
            <p class="LatinA"><span class="Categorie">Nom Latin</span> {{ article.namelatin }}</p>
            <p class="ToxiciteA"><span class="Categorie">Toxicité</span> {{ article.toxicite }}</p>
            <p class="EnvironnementA"><span class="Categorie">Environnement</span> {{ article.environnement }}</p>
            <p class="AchatA"><span class="Categorie">Lien D'achat</span> {{ article.urlBuy }}</p>
            <p class="DescriptionU"><span class="Categorie">Activé</span> {{ article.active ? 'Oui' : 'Non' }}</p>
            <p><span class="Categorie">Actions</span>
                <a href="{{ path('articles_show', {'id': article.id}) }}" class="ReturnList">Voir</a>
                <a href="{{ path('articles_edit', {'id': article.id}) }}" class="ReturnList">Modifier</a>
            </p>
        </div>
    {% else %}
        <p>Aucun résultat trouvé</p>
    {% endfor %}
    <div class="navigation">
        {{ knp_pagination_render(articles) }}
    </div>
    <div class="Center">
        <a href="{{ path('create_article_admin') }}" class="ReturnList">Création d'un nouveau</a>
    </div>
{% endblock %}