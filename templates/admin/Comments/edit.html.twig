{% extends "base.html.twig" %}

{% block body %}
    {% if app.user == comment.users %}
        {% if is_granted('ROLE_USER') %}
            {{ form(form) }}
            {{ include('admin/Comments/deleteM.html.twig') }}
            <a href="{{ path('homepage_member') }}">Retourner sur la liste</a>
        {% endif %}
    {% else %}
        <h1> Tu n'as pas accès à cette page </h1>
    {% endif %}
    {% if is_granted('ROLE_ADMIN') %}
    {{ form(form) }}
    {{ include('admin/Comments/delete.html.twig') }}
    <a href="{{ path('comments') }}">Retourner sur la liste</a>
    {% endif %}
{% endblock %}